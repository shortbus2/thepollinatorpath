(()=>{const tasks=window.GARDEN_TASK_ENGINE.list();const filter=new URLSearchParams(location.search).get('filter');const visible=filter?tasks.filter(t=>t.type===filter||(filter==='observations'&&t.type==='places')):tasks;const counts=[['All tasks',tasks.length],['Portraits',tasks.filter(t=>t.type==='portraits').length],['Map',tasks.filter(t=>t.type==='map').length],['Observations',tasks.filter(t=>['observations','places','walks'].includes(t.type)).length]];document.querySelector('#taskSummary').innerHTML=counts.map(([a,b])=>`<div class="metric-card"><span>${a}</span><strong>${b}</strong></div>`).join('');document.querySelector('#taskList').innerHTML=visible.map(t=>`<a class="prompt" href="${t.href}"><span>→</span>${t.text}</a>`).join('')||'<div class="prompt">Everything current. Go enjoy the garden. 💚</div>';})();