const API_VERSION="2022-11-28";const MAX_PHOTOS=20,MAX_BASE64_CHARS=12_000_000,WORKER_VERSION="3.2.0-stable-test";
const json=(value,status=200,headers={})=>new Response(JSON.stringify(value),{status,headers:{"content-type":"application/json; charset=utf-8","cache-control":"no-store","x-content-type-options":"nosniff","referrer-policy":"no-referrer",...headers}});
function cors(env,request){const origin=request.headers.get("origin")||"",allowed=String(env.ALLOWED_ORIGIN||"").split(",").map(x=>x.trim()).filter(Boolean),accepted=!origin||allowed.includes(origin);return{"access-control-allow-origin":accepted?origin:(allowed[0]||"null"),"access-control-allow-headers":"authorization,content-type","access-control-allow-methods":"GET,POST,OPTIONS",vary:"Origin"}}
const authenticated=(request,env)=>request.headers.get("authorization")===`Bearer ${env.NOTEBOOK_KEY}`;
const safeSlug=(value,fallback="entry")=>String(value||fallback).toLowerCase().replace(/[^a-z0-9.-]+/g,"-").replace(/^-|-$/g,"").slice(0,96)||fallback;
function utf8FromBase64(value){const binary=atob(String(value||"").replace(/\n/g,"")),bytes=Uint8Array.from(binary,c=>c.charCodeAt(0));return new TextDecoder().decode(bytes)}
async function github(env,path,init={}){const response=await fetch(`https://api.github.com/repos/${env.GITHUB_OWNER}/${env.GITHUB_REPO}${path}`,{...init,headers:{accept:"application/vnd.github+json",authorization:`Bearer ${env.GITHUB_TOKEN}`,"x-github-api-version":API_VERSION,"user-agent":"pollinator-path-garden-brain",...(init.headers||{})}});if(!response.ok)throw new Error(`GitHub ${response.status}: ${await response.text()}`);return response}
async function branchState(env){const branch=String(env.GITHUB_BRANCH||"main").replace(/^refs\/heads\//,"");const ref=await(await github(env,`/git/ref/heads/${encodeURIComponent(branch)}`)).json(),commitSha=ref.object.sha,commit=await(await github(env,`/git/commits/${commitSha}`)).json();return{branch,commitSha,treeSha:commit.tree.sha}}
async function getTextFile(env,path){const branch=String(env.GITHUB_BRANCH||"main").replace(/^refs\/heads\//,"");const response=await fetch(`https://api.github.com/repos/${env.GITHUB_OWNER}/${env.GITHUB_REPO}/contents/${path}?ref=${encodeURIComponent(branch)}`,{headers:{accept:"application/vnd.github+json",authorization:`Bearer ${env.GITHUB_TOKEN}`,"x-github-api-version":API_VERSION,"user-agent":"pollinator-path-garden-brain"}});if(response.status===404)return null;if(!response.ok)throw new Error(`GitHub ${response.status}: ${await response.text()}`);return utf8FromBase64((await response.json()).content)}
async function commitOnce(env,files,message){if(!files.length)throw Error("No files supplied for commit");const{branch,commitSha,treeSha}=await branchState(env),tree=[];for(const file of files){if(file.encoding==="base64"){const blob=await(await github(env,"/git/blobs",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({content:file.content,encoding:"base64"})})).json();tree.push({path:file.path,mode:"100644",type:"blob",sha:blob.sha})}else tree.push({path:file.path,mode:"100644",type:"blob",content:file.content})}const newTree=await(await github(env,"/git/trees",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({base_tree:treeSha,tree})})).json(),newCommit=await(await github(env,"/git/commits",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({message,tree:newTree.sha,parents:[commitSha]})})).json();await github(env,`/git/refs/heads/${encodeURIComponent(branch)}`,{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify({sha:newCommit.sha,force:false})});return{commitSha:newCommit.sha,changedFiles:files.length}}
async function atomicCommit(env,files,message){let last;for(let attempt=1;attempt<=6;attempt++){try{return await commitOnce(env,files,message)}catch(error){last=error;const retry=/GitHub 409|GitHub 422|Reference cannot be updated|is at [a-f0-9]+ but expected/i.test(String(error.message||error));if(!retry||attempt===6)break;await new Promise(r=>setTimeout(r,300*2**(attempt-1)+Math.floor(Math.random()*250)))}}if(/GitHub 422|Reference cannot be updated/i.test(String(last?.message||last)))throw Error("GitHub changed while Garden Brain was publishing. Six safe retries could not fast-forward the branch. Wait a few seconds and try once more. If it repeats, confirm the Worker uses branch 'main' and that the token can write to that branch.");throw last}
function parseWindowValue(text,name,fallback){if(!text)return fallback;const escaped=name.replace(/[.*+?^${}()|[\]\\]/g,"\\$&"),match=text.match(new RegExp(`window\\.${escaped}\\s*=\\s*([\\s\\S]*?);(?:\\s|$)`));if(!match)return fallback;try{return JSON.parse(match[1])}catch{return fallback}}
const parseArray=(text,name)=>{const x=parseWindowValue(text,name,[]);return Array.isArray(x)?x:[]},parseObject=(text,name)=>{const x=parseWindowValue(text,name,{});return x&&typeof x==="object"&&!Array.isArray(x)?x:{}};
const serialize=(name,value,comment="")=>`${comment?`// ${comment}\n`:""}window.${name} = ${JSON.stringify(value,null,2)};\n`;
function cleanPublicEntry(entry,photoPaths){const clean={...entry,photos:photoPaths};delete clean.privateNotes;delete clean.rawPhotos;delete clean.notebookKey;return clean}
async function publishObservation(env,entry){if(!entry?.id||!entry?.date)throw Error("Missing entry id or date");if(entry.public!==true)throw Error("Private entries cannot be published to the public repository");if(!entry.privacyReview?.completed)throw Error("Privacy review is required before publication");const photos=Array.isArray(entry.photos)?entry.photos:[];if(photos.length>MAX_PHOTOS)throw Error(`Maximum ${MAX_PHOTOS} photos per entry`);const files=[],photoPaths=[],manifestText=await getTextFile(env,"image-manifest.js"),manifest=parseObject(manifestText,"IMAGE_MANIFEST");manifest.hero=Array.isArray(manifest.hero)?manifest.hero:[];manifest.plants=manifest.plants&&typeof manifest.plants==="object"?manifest.plants:{};manifest.wildlife=manifest.wildlife&&typeof manifest.wildlife==="object"?manifest.wildlife:{};let changed=false;for(let i=0;i<photos.length;i++){const raw=String(photos[i].data||"").split(",")[1];if(!raw)continue;if(raw.length>MAX_BASE64_CHARS)throw Error("A prepared photo is too large");const primary=entry.primary||{},folder=`images/observations/${String(entry.date).slice(0,4)}/${safeSlug(entry.id)}`,filename=safeSlug(photos[i].name||`photo-${i+1}.jpg`,`${i+1}.jpg`),path=`${folder}/${filename.endsWith(".jpg")?filename:filename+".jpg"}`;files.push({path,content:raw,encoding:"base64"});photoPaths.push(path);if(photos[i].hero){let heroFolder="",group="",key="";if(primary.kind==="plant"){heroFolder=`images/plants/${safeSlug(primary.id)}`;group="plants";key=String(primary.id)}if(["visitor","resident"].includes(primary.kind)){heroFolder=`images/wildlife/${safeSlug(primary.id)}`;group="wildlife";key=String(primary.id)}if(primary.kind==="object")heroFolder=`images/objects/${safeSlug(primary.id)}`;if(heroFolder){const heroPath=`${heroFolder}/hero.jpg`;files.push({path:heroPath,content:raw,encoding:"base64"});if(group){manifest[group][key]={...(manifest[group][key]||{}),hero:heroPath};changed=true}}}if(entry.featured&&entry.privacyReview?.safeHomepage&&i===0&&!manifest.hero.includes(path)){manifest.hero.unshift(path);manifest.hero=manifest.hero.slice(0,12);changed=true}}
let observations=parseArray(await getTextFile(env,"observations.js"),"OBSERVATIONS"),publicEntry=cleanPublicEntry(entry,photoPaths);observations=observations.filter(x=>x.id!==publicEntry.id);observations.unshift(publicEntry);files.push({path:"observations.js",content:serialize("OBSERVATIONS",observations),encoding:"utf8"});if(changed)files.push({path:"image-manifest.js",content:serialize("IMAGE_MANIFEST",manifest,"Automatically maintained by Garden Brain publishing."),encoding:"utf8"});return atomicCommit(env,files,`Garden Brain: ${entry.title||entry.id}`)}
async function publishArray(env,{path,variable,items,message,comment}){if(!Array.isArray(items))throw Error("Expected an array");return atomicCommit(env,[{path,content:serialize(variable,items,comment),encoding:"utf8"}],message)}
export default{async fetch(request,env){const headers=cors(env,request);if(request.method==="OPTIONS")return new Response(null,{headers});const url=new URL(request.url);if(url.pathname==="/health"&&request.method==="GET")return json({ok:true,service:"Pollinator Path Garden Brain",version:WORKER_VERSION},200,headers);if(!authenticated(request,env))return json({error:"Unauthorized"},401,headers);try{if(url.pathname==="/garden"&&request.method==="GET"){const[p,o,m,r]=await Promise.all([getTextFile(env,"placements.js"),getTextFile(env,"observations.js"),getTextFile(env,"milestones.js"),getTextFile(env,"residents.js")]);return json({placements:parseArray(p,"GARDEN_PLACEMENTS"),observations:parseArray(o,"OBSERVATIONS"),milestones:parseArray(m,"GARDEN_MILESTONES"),residents:parseArray(r,"GARDEN_RESIDENTS")},200,headers)}if(["/entry","/observations"].includes(url.pathname)&&request.method==="POST")return json({ok:true,...await publishObservation(env,await request.json())},200,headers);if(url.pathname==="/placements"&&request.method==="POST"){const b=await request.json();return json({ok:true,...await publishArray(env,{path:"placements.js",variable:"GARDEN_PLACEMENTS",items:b.placements,message:"Garden Brain: publish map placements",comment:"Automatically published by the Garden Map Editor."})},200,headers)}if(url.pathname==="/residents"&&request.method==="POST"){const b=await request.json();return json({ok:true,...await publishArray(env,{path:"residents.js",variable:"GARDEN_RESIDENTS",items:b.residents,message:"Garden Brain: update residents",comment:"Editable named garden residents."})},200,headers)}if(url.pathname==="/milestones"&&request.method==="POST"){const b=await request.json();return json({ok:true,...await publishArray(env,{path:"milestones.js",variable:"GARDEN_MILESTONES",items:b.milestones,message:"Garden Brain: update milestones",comment:"Garden milestones and meaningful firsts."})},200,headers)}return json({error:"Not found"},404,headers)}catch(error){return json({error:error.message},500,headers)}}};