{
  "name": "pollinator-path-garden-brain-worker",
  "version": "4.4.0-rc.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test:foundation-contract": "node ../scripts/test-foundation-write-contract.mjs",
    "validate:staging": "node ../scripts/validate-foundation-release.mjs --worker-contract"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
