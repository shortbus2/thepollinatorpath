# Architecture

Garden Walk → Garden Brain private memory → organization and review → privacy review → explicitly published material → The Pollinator Path public story.

The current stable-test release uses a public GitHub repository only for explicitly published records. Device-only drafts are not a durable private archive. A future private storage layer must remain separate from the public repository.
