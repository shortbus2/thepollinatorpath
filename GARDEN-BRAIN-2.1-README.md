# The Pollinator Path 2.1 — Automated Garden Brain

New in 2.1:
- Map editor can publish placements directly to GitHub.
- Field Notebook can publish observations and optimized photos directly to GitHub.
- `placements.js` is the automatically managed map layer.
- Cloudflare Worker provides authenticated `/entry`, `/placements`, and `/health` endpoints.
- Export JSON remains available as a backup, not the normal workflow.

Complete `AUTOMATION-SETUP.md` after installing this package.
