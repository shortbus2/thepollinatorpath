(()=>{const B=structuredClone(window.GARDEN_BRAIN),$=s=>document.querySelector(s),map=$('#e-map'),kind=$('#e-kind'),sub=$('#e-subject'),img=$('#e-image'),layer=$('#e-layer'),stage=$('#e-stage'),status=$('#e-status');const key='tpp-map-edits-v2';let placements=JSON.parse(localStorage.getItem(key)||'null')||B.placements;B.maps.forEach(m=>map.add(new Option(m.name,m.id)));function subjects(){sub.innerHTML='';if(kind.value==='plant')PLANTS.forEach(p=>sub.add(new Option(`#${p.number} · ${p.common}`,p.number)));else B.objects.forEach(o=>sub.add(new Option(`${o.name} · ${o.type}`,o.id)))}function label(p){if(p.kind==='plant')return p.plantNumber;return '●'}function save(){localStorage.setItem(key,JSON.stringify(placements));status.textContent='Saved on this device.'}function render(){const m=B.maps.find(x=>x.id===map.value);img.src=m.image;layer.innerHTML=placements.filter(p=>p.map===map.value).map(p=>`<button class="map-marker ${p.kind}" style="left:${p.x}%;top:${p.y}%" data-id="${p.id}">${label(p)}</button>`).join('')}function point(ev){const r=stage.getBoundingClientRect();return{x:Math.max(0,Math.min(100,(ev.clientX-r.left)/r.width*100)),y:Math.max(0,Math.min(100,(ev.clientY-r.top)/r.height*100))}}stage.addEventListener('click',e=>{if(e.target.closest('.map-marker'))return;const q=point(e);placements.push({id:'p-'+Date.now(),kind:kind.value,map:map.value,x:+q.x.toFixed(2),y:+q.y.toFixed(2),quantity:+$('#e-qty').value||1,precision:$('#e-precision').value,status:'active',...(kind.value==='plant'?{plantNumber:+sub.value}:{objectId:sub.value})});save();render()});let dragging=null;layer.addEventListener('pointerdown',e=>{const b=e.target.closest('.map-marker');if(!b)return;dragging=placements.find(p=>p.id===b.dataset.id);b.setPointerCapture(e.pointerId)});layer.addEventListener('pointermove',e=>{if(!dragging)return;const q=point(e);dragging.x=+q.x.toFixed(2);dragging.y=+q.y.toFixed(2);render()});layer.addEventListener('pointerup',()=>{if(dragging){save();dragging=null}});map.addEventListener('change',render);kind.addEventListener('change',subjects);$('#reset').addEventListener('click',()=>{localStorage.removeItem(key);placements=structuredClone(B.placements);render();status.textContent='Local edits reset.'});$('#export').addEventListener('click',()=>{const blob=new Blob([JSON.stringify({version:B.version,placements},null,2)],{type:'application/json'}),a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download='garden-placements-update.json';a.click();URL.revokeObjectURL(a.href);status.textContent='Exported. Keep this file for the publishing step.'});subjects();render()})();